== Welcome to Rails

Rails app that allows anonymous users to upload files

It should have tests or specs, ideally constructed with a test driven approach.

Bonus points if it warns a user when they are trying to upload a file with identical contents to a file that has already been uploaded.